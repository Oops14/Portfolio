import styled from "styled-components";

export const HeaderMain = styled.header`
    position: absolute;
    left: 0;
    right: 0;
    height: 100px;

    display: flex;
    align-items: center;
`;

